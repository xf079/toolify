import { DEFAULT_SETTINGS_KEY } from './constants';

const defaultSettings = {
  type: DEFAULT_SETTINGS_KEY,
  theme: 'system',
  colorPrimary: '#00A76F',
  start: false,
  guide: false,
  language: 'zh-CN',
  placeholder: 'Hi, Toolify'
};


export default defaultSettings